# ════════════════════════════════════════════════════════════════════════════
# CONFIGURAÇÕES DE AMBIENTE - KT TRANSCRIBER
# ════════════════════════════════════════════════════════════════════════════
# Copie este arquivo para .env e configure com seus valores reais

# ════════════════════════════════════════════════════════════════════════════
# CONFIGURAÇÃO OBRIGATÓRIA (aplicação não inicia se ausente)
# ════════════════════════════════════════════════════════════════════════════

# Encoding UTF-8 para todos os processos Python (OBRIGATÓRIO no Windows)
PYTHONUTF8=1

# Application Environment (OBRIGATÓRIO)
# Valores válidos: development | staging | production
APP_ENVIRONMENT=development

# ════════════════════════════════════════════════════════════════════════════
# TL:DV API (OBRIGATÓRIO)
# ════════════════════════════════════════════════════════════════════════════

TLDV_API_KEY=your-tldv-api-key-here  # OBRIGATÓRIO
TLDV_BASE_URL=https://pasta.tldv.io/v1alpha1  # Padrão: URL da API TL:DV
TLDV_TIMEOUT=60  # Padrão: 60s — timeout para chamadas à API TL:DV

# ════════════════════════════════════════════════════════════════════════════
# OPENAI (OBRIGATÓRIO — LLM + Embeddings)
# ════════════════════════════════════════════════════════════════════════════

OPENAI_API_KEY=sk-your-openai-api-key-here  # OBRIGATÓRIO
OPENAI_MODEL=gpt-4o-mini  # Padrão: modelo para geração de metadados e RAG
OPENAI_MAX_TOKENS=4096  # Padrão: máximo de tokens por resposta
OPENAI_EMBEDDING_MODEL=text-embedding-3-small  # Padrão: modelo de embeddings

# ════════════════════════════════════════════════════════════════════════════
# REDIS (OBRIGATÓRIO — ARQ job queue)
# ════════════════════════════════════════════════════════════════════════════
# Redis Cloud — configure com suas credenciais do Redis Cloud
# Acesse: https://app.redislabs.com → Database → Configuration

REDIS_HOST=redis-xxxxx.c1.us-east-1-2.ec2.redns.redis-cloud.com  # OBRIGATÓRIO
REDIS_PORT=12345  # OBRIGATÓRIO
REDIS_PASSWORD=your-redis-cloud-password  # OBRIGATÓRIO
REDIS_DB=0  # Padrão: 0

# ════════════════════════════════════════════════════════════════════════════
# CHROMADB (OPCIONAL — usa defaults se ausente)
# ════════════════════════════════════════════════════════════════════════════

CHROMA_COLLECTION_NAME=kt_transcriptions  # Padrão: nome da coleção no ChromaDB

# ════════════════════════════════════════════════════════════════════════════
# STREAMLIT (OPCIONAL — usa default se ausente)
# ════════════════════════════════════════════════════════════════════════════

FASTAPI_URL=http://localhost:8000  # Padrão: URL base da FastAPI consumida pelo Streamlit

# ════════════════════════════════════════════════════════════════════════════
# CONFIGURAÇÕES OPCIONAIS
# ════════════════════════════════════════════════════════════════════════════

# Logging
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# Processing
MAX_RETRIES=3  # Padrão: 3 tentativas em falhas
REQUEST_TIMEOUT=30  # Padrão: 30s timeout para requests HTTP
BATCH_SIZE=10  # Padrão: lote de processamento
